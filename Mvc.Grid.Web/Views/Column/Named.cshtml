@model IEnumerable<PersonModel>

@{
    ViewBag.Title = "Column » Name";
}

<ul class="top-nav">
    <li>Column »</li>
    <li>Name</li>
</ul>
<hr>

<div class="doc">
    <div id="getting-started" class="section">
        <h2>Description</h2>
        <p>
            Sets column name.
        </p>
        <h2>Remarks</h2>
        <p>By default, column name is expression property names joined by dot.</p>
        <p>Column name is used for column filtering and sorting. And is visible in the url.</p>
        <p>Because of that filterable/sortable columns shouyld have unique names.</p>
        <h2>Examples</h2>
        <pre class="example-code"><a class="show-code">Show/Hide the code...</a>
<span class="code">
@@(
    Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled(<span class="str">"Name"</span>).Named(<span class="str">"N"</span>).Sortable(<span class="kwd">true</span>);
            columns.Add(model => model.Surname).Titled(<span class="str">"Surname"</span>).Sortable(<span class="kwd">true</span>);

            columns.Add(model => model.Age).Titled(<span class="str">"Age"</span>);
            columns.Add(model => model.Birthday).Titled(<span class="str">"Birth date"</span>);
            columns.Add(model => model.IsWorking).Titled(<span class="str">"Employed"</span>);
        })
)</span></pre>
    </div>
</div>

@(
 Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled("Name").Named("N").Sortable(true);
            columns.Add(model => model.Surname).Titled("Surname").Sortable(true);

            columns.Add(model => model.Age).Titled("Age");
            columns.Add(model => model.Birthday).Titled("Birth date");
            columns.Add(model => model.IsWorking).Titled("Employed");
        })
)
