@model IEnumerable<PersonModel>

@{
    ViewBag.Title = "Column » Filtered as";
}

<ul class="top-nav">
    <li>Column »</li>
    <li>Filtered as</li>
</ul>
<hr>

<div class="doc">
    <div id="getting-started" class="section">
        <h2>Description</h2>
        <p>
            Specifies filter name to be used for rendering filter popup on given column.
        </p>
        <h2>Remarks</h2>
        <p>By default filter name is set depending on column property type.</p>
        <p>Specifying custom filter name will only work if filter name is passes to mvcgrid constructor as an filter option.</p>
        <h2>Examples</h2>
        <pre class="example-code"><a class="show-code">Show/Hide the code...</a>
<span class="code">
@@(
    Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled(<span class="str">"Name"</span>).Filterable(<span class="kwd">true</span>).FilteredAs(<span class="str">"Text"</span>);
            columns.Add(model => model.Surname).Titled(<span class="str">"Surname"</span>);
            columns.Add(model => model.Age).Titled(<span class="str">"Age"</span>);
            columns.Add(model => model.Birthday).Titled(<span class="str">"Birth day"</span>);
            columns.Add(model => model.IsWorking).Titled(<span class="str">"Employed"</span>);
        })
)</span></pre>
    </div>
</div>

@(
 Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled("Name").Filterable(true).FilteredAs("Text");
            columns.Add(model => model.Surname).Titled("Surname");

            columns.Add(model => model.Age).Titled("Age");
            columns.Add(model => model.Birthday).Titled("Birth date");
            columns.Add(model => model.IsWorking).Titled("Employed");
        })
)