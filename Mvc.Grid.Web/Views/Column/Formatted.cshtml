@model IEnumerable<PersonModel>

@{
    ViewBag.Title = "Column » Formatted";
}

<ul class="top-nav">
    <li>Column »</li>
    <li>Formatted</li>
</ul>
<hr>

<div class="doc">
    <div id="getting-started" class="section">
        <h2>Description</h2>
        <p>
            Specifies column value format.
        </p>
        <h2>Remarks</h2>
        <p>By default, column is not formatted in any way.</p>
        <p>Specified format will be used in String.Format(format, args) method to format column value.</p>
        <p><b>Be careful:</b> value expression specified by Add(...) method will still be used for filtering and sorting!</p>
        <h2>Examples</h2>
        <pre class="example-code"><a class="show-code">Show/Hide the code...</a>
<span class="code">
@@(
    Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled(<span class="str">"Name"</span>);
            columns.Add(model => model.Surname).Titled(<span class="str">"Surname"</span>);

            columns.Add(model => model.Age).Titled(<span class="str">"Age"</span>);
            columns.Add(model => model.Birthday).Titled(<span class="str">"Birth year"</span>).Formatted(<span class="str">"{0: yyyy}"</span>);
            columns.Add(model => model.IsWorking).Titled(<span class="str">"Employed"</span>);
        })
)</span></pre>
    </div>
</div>

@(
 Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled("Name");
            columns.Add(model => model.Surname).Titled("Surname");

            columns.Add(model => model.Age).Titled("Age");
            columns.Add(model => model.Birthday).Titled("Birth year").Formatted("{0: yyyy}");
            columns.Add(model => model.IsWorking).Titled("Employed");
        })
)