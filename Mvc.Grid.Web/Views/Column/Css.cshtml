@model IEnumerable<PersonModel>

@{
    ViewBag.Title = "Column » Css";
}

<ul class="top-nav">
    <li>Column »</li>
    <li>Css</li>
</ul>
<hr>

<div class="doc">
    <div id="getting-started" class="section">
        <h2>Description</h2>
        <p>
            Sets column classes, used for css or js.
        </p>
        <h2>Remarks</h2>
        <p>By default, column css is null.</p>
        <p>Specified column classes are applied for column header and each of it's row cells.</p>
        <h2>Examples</h2>
        <pre class="example-code"><a class="show-code">Show/Hide the code...</a>
<span class="code">
@@(
    Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled(<span class="str">"Name"</span>);
            columns.Add(model => model.Surname).Titled(<span class="str">"Surname"</span>);

            columns.Add(model => model.Age).Titled(<span class="str">"Bolded age"</span>).Css(<span class="str">"bolded"</span>);
            columns.Add(model => model.Birthday).Titled(<span class="str">"Birth date"</span>);
            columns.Add(model => model.IsWorking).Titled(<span class="str">"Employed"</span>);
        })
)

&lt;style&gt;
    .bolded {
        font-weight: bold;
    }
&lt;/style&gt;</span></pre>
    </div>
</div>

@(
 Html
        .Grid(Model)
        .Build(columns =>
        {
            columns.Add(model => model.Name).Titled("Name");
            columns.Add(model => model.Surname).Titled("Surname");

            columns.Add(model => model.Age).Titled("Bolded age").Css("bolded");
            columns.Add(model => model.Birthday).Titled("Birth date");
            columns.Add(model => model.IsWorking).Titled("Employed");
        })
)

<style>
    .bolded {
        font-weight: bold;
    }
</style>